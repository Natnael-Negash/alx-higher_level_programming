#!/usr/bin/node
// define the rectangle class with width and height attribute
modules.exports = class Rectangle {
    constructor (width,height) {
        this.width = width;
        this.height = height;
    }
};