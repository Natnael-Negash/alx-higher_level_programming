#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    newsquare = []
    a = 0

    for x in matrix:
        newsquare.append([])

        for y in matrix[a]:
            newsquare[a}.append(y**2)
